# Network Scanner Configuration

# Secret key for session management
SECRET_KEY=your-secret-key-here

# Scan interval in seconds (default: 3600 = 1 hour)
SCAN_INTERVAL=3600

# Host subnet to scan when running in Docker
# IMPORTANT: Set this to your actual network subnet for accurate scanning
# Without this, the scanner may default to a generic subnet that doesn't match your network
# 
# To find your network subnet:
# 1. On Windows: run 'ipconfig' and look at your IP address and subnet mask
# 2. On Mac/Linux: run 'ifconfig' or 'ip route' to see your network
# 3. Most home networks use 192.168.1.0/24 or 192.168.0.0/24
#
# Examples:
#   - 192.168.1.0/24 for typical home networks (192.168.1.1-192.168.1.254)
#   - 192.168.0.0/24 for some home networks (192.168.0.1-192.168.0.254)  
#   - 10.0.0.0/24 for some corporate networks (10.0.0.1-10.0.0.254)
#   - 172.16.0.0/24 for other private networks (172.16.0.1-172.16.0.254)
HOST_SUBNET=192.168.1.0/24

# Database path (optional, defaults to /app/data/enhanced_inventory.db)
# DATABASE_PATH=/app/data/enhanced_inventory.db

# Scan results path (optional, defaults to /app/data/scans)
# SCAN_RESULTS_PATH=/app/data/scans